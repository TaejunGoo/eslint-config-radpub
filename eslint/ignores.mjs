/**
 * ESLint 무시 설정
 *
 * 린트에서 제외할 파일 및 디렉토리
 * - 빌드 결과물 (dist, build, .next)
 * - 의존성 (node_modules)
 * - 생성된 파일
 * - 캐시 디렉토리
 */
const ignoresConfig = [
  {
    ignores: [
      // ============================================
      // 빌드 결과물
      // ============================================
      "build/*",
      "dist/*",
      "**/.next/*",

      // ============================================
      // 의존성
      // ============================================
      "**/node_modules/*",

      // ============================================
      // 캐시 디렉토리
      // ============================================
      ".cache/*",
      "*.cache",

      // ============================================
      // 생성된 파일
      // ============================================
      "*.generated.*",

      // ============================================
      // 환경 변수 파일 (선택적, .gitignore에 이미 포함됨)
      // ============================================
      // ".env*",
    ],
  },
];

export default ignoresConfig;
